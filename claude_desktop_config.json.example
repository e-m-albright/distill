{
  "mcpServers": {
    "distillation": {
      "command": "uv",
      "args": ["run", "--project", "/path/to/distillation", "python", "-m", "app.mcp_server"],
      "cwd": "/path/to/distillation"
    }
  }
}
